## Packages
zustand | Global state management for the shopping cart
react-leaflet | Map component for Locations page
leaflet | Core mapping library
framer-motion | Smooth animations for UI transitions

## Notes
Leaflet CSS must be imported in index.css
Map tiles will use OpenStreetMap (free, no API key required)
Cart state persists in memory (could be persisted to localStorage via middleware if needed later)
